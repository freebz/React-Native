// 예제 6-16 <LocationButton>에서 사용하는 스타일

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    locationButton: {
	backgroundColor: '#FFDDFF',
	width: 200,
	padding: 25,
	borderRadius: 5
    },
});

export default styles;
